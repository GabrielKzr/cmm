#include <stdio.h>
#include "asdr_sample.h"
// #include "lexer.h"

/*
uint8_t lexer_push(void *list, void *data)
{
    return llist_push_back((struct llist_t *)list, data);
}

uint8_t syntatic_pop(void *list, void *data)
{
    return llist_pop_front((struct llist_t *)list); // pop front is what makes it to be left most derivation
}
*/

int main(void)
{
    prog();    

    return 0;
}