$TRACE_ON

int x, y, z;
double media;

func int soma(int a, int b) {
    int resultado;

    resultado = a + b;
    x = resultado;
}

func void processa(int n) {
    int i;
    int total;

    i = 1;
    total = 0;

    while (i) {
        total = total + i;
        i = i - 1;
    }

    if (total) {
        x = total * 2;
    } else {
        x = total / 2;
    }

    if (i)
        y = i;
}